import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  gap: 10px;
  width: auto;
`;

export const Label = styled.label`
  width: auto;
  display: flex;
  gap: 20px;
  margin: 10px;
`;
